import { blogPost } from "./blogPost";
import { homePage } from "./homePage";
import { callToAction, featureGrid3, imageText } from "./homeSections";

export const schemaTypes = [featureGrid3, imageText, callToAction, homePage, blogPost];
